"use strict";

/* let numberFirst = 0.1;
let numberSecond = 0.2;
let sum = numberFirst + numberSecond;
alert(sum.toFixed(1)); */

/* let string = "1";
let number = 2;
let sum = +string + number;
alert(sum); */

/* const storageFile = 820;
let storageSpace = parseFloat(prompt("How is storage space(GB) ?", ''));
alert(`You will be able to store: ${Math.floor(storageSpace * 1000 / storageFile)} files of 820 MB.`); */

/* let sum = parseFloat(prompt("How much do you have?", ''));
let candyPrice = parseFloat(prompt("How much is a candy bar?", ''));
let amountСhocolates = sum / candyPrice;
let change = sum - candyPrice * Math.floor(amountСhocolates);
alert(`You can buy: ${Math.floor(amountСhocolates)}, change: ${change.toFixed(2)}`); */

/* let depositAmount = prompt("How much of your deposit?", '');
let months = prompt("For how many months you make a deposit?", '') 
const interest = 0.05;
let result = depositAmount * (1 + interest) ** (months / 12) - depositAmount;
alert (`Your income will be: ${result.toFixed(2)}`); */

// 2 && 0 && 3; // 0;
// 2 || 0 || 3; // 2;
// 2 && 0 || 3; // 3;
