/* const max = 100; 
const numbers = new Array(50).fill(0).map(
    () => Math.floor(Math.random() * max)
);

console.log(numbers);

function getEvenNumbers(array) {
    return array.filter(
        (element) => element % 2 === 0
    );
}

function getNumbersInRange (array, min, max) {
    return array.filter (
        (element) => element >= min && element <= max
    );
}

console.log(getNumbersInRange(numbers, 90, 100)); */